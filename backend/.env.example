PORT=5000
GROQ_API_KEY=your_groq_api_key_here
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_gmail_app_password_here

# IMPORTANT: For Gmail, you need to use an App Password, not your regular password
# 1. Go to Google Account → Security → 2-Step Verification (enable if not already)
# 2. Go to Google Account → Security → App passwords
# 3. Generate an app password for "Mail"
# 4. Replace EMAIL_PASS above with the generated app password (16 characters)